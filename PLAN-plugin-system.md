# 플러그인 시스템 설계 계획

## 개요

SideDish에 **플러그인 구독 시스템**을 추가합니다.
- 관리자가 플러그인 생성 (날씨, 뉴스, 코인 등)
- 사용자가 원하는 플러그인 구독
- 매일 정해진 시간에 이메일로 요약 브리핑 발송

---

## 1. 데이터베이스 스키마

### 1.1 Firestore Collections

```
plugins/                    # 플러그인 정의
  {pluginId}/
    - id: string
    - name: string          # "오늘의 날씨"
    - slug: string          # "weather" (URL용)
    - description: string   # 플러그인 설명
    - icon: string          # 이모지 "☀️"
    - category: string      # "weather" | "finance" | "news" | ...
    - isActive: boolean     # 활성화 여부
    - isPremium: boolean    # 유료 여부
    - config: object        # 플러그인별 설정 (API 키 등은 서버에만)
      - cities?: string[]   # 날씨: 지원 도시 목록
      - sendTime: string    # "07:00" 발송 시간 (KST)
    - createdAt: Timestamp
    - updatedAt: Timestamp

subscriptions/              # 사용자 구독 정보
  {subscriptionId}/
    - id: string
    - userId: string        # 구독한 사용자
    - userEmail: string     # 이메일 발송용
    - pluginId: string      # 구독한 플러그인
    - settings: object      # 사용자별 설정
      - city?: string       # 날씨: 선택한 도시
      - detailMode?: boolean # 상세 모드 여부 (기본 false)
    - isActive: boolean     # 구독 활성 상태
    - createdAt: Timestamp
    - updatedAt: Timestamp

plugin_logs/                # 발송 로그 (디버깅/통계용)
  {logId}/
    - id: string
    - pluginId: string
    - executedAt: Timestamp
    - subscriberCount: number
    - successCount: number
    - failureCount: number
    - generatedContent: string  # AI 생성 결과 (캐시)
```

### 1.2 TypeScript 타입 정의

```typescript
// src/lib/plugin-types.ts

// 플러그인 카테고리
type PluginCategory = 'weather' | 'finance' | 'news' | 'lifestyle' | 'other'

// 지원 도시
type SupportedCity = 'seoul' | 'busan' | 'daegu' | 'incheon' | 'daejeon' | 'gwangju'

// Firestore 문서 타입
interface PluginDoc {
  id: string
  name: string
  slug: string
  description: string
  icon: string
  category: PluginCategory
  isActive: boolean
  isPremium: boolean
  config: {
    cities?: SupportedCity[]
    sendTime: string  // "HH:mm" KST
  }
  createdAt: Timestamp
  updatedAt: Timestamp
}

interface SubscriptionDoc {
  id: string
  userId: string
  userEmail: string
  pluginId: string
  settings: {
    city?: SupportedCity
    detailMode?: boolean
  }
  isActive: boolean
  createdAt: Timestamp
  updatedAt: Timestamp
}

// API 응답 타입
interface PluginResponse {
  id: string
  name: string
  slug: string
  description: string
  icon: string
  category: PluginCategory
  isActive: boolean
  isPremium: boolean
  config: {
    cities?: SupportedCity[]
    sendTime: string
  }
  subscriberCount?: number  // 구독자 수 (옵션)
  isSubscribed?: boolean    // 현재 유저 구독 여부
  createdAt: string
  updatedAt: string
}

interface SubscriptionResponse {
  id: string
  plugin: PluginResponse
  settings: {
    city?: SupportedCity
    detailMode?: boolean
  }
  isActive: boolean
  createdAt: string
}
```

---

## 2. API 엔드포인트

### 2.1 플러그인 관련

| Endpoint | Method | Auth | 설명 |
|----------|--------|------|------|
| `/api/plugins` | GET | No | 활성 플러그인 목록 |
| `/api/plugins` | POST | Admin | 플러그인 생성 |
| `/api/plugins/[id]` | GET | No | 플러그인 상세 |
| `/api/plugins/[id]` | PATCH | Admin | 플러그인 수정 |
| `/api/plugins/[id]` | DELETE | Admin | 플러그인 삭제 |

### 2.2 구독 관련

| Endpoint | Method | Auth | 설명 |
|----------|--------|------|------|
| `/api/subscriptions` | GET | Yes | 내 구독 목록 |
| `/api/subscriptions` | POST | Yes | 플러그인 구독 |
| `/api/subscriptions/[id]` | PATCH | Yes | 구독 설정 수정 |
| `/api/subscriptions/[id]` | DELETE | Yes | 구독 취소 |

### 2.3 실행/발송 관련

| Endpoint | Method | Auth | 설명 |
|----------|--------|------|------|
| `/api/plugins/[id]/execute` | POST | Cron | 플러그인 실행 (스케줄러용) |
| `/api/plugins/[id]/preview` | GET | Yes | 오늘의 브리핑 미리보기 |

---

## 3. 페이지 구조

### 3.1 새로운 페이지

```
src/app/
├── plugins/
│   ├── page.tsx              # 플러그인 목록 (구독 가능)
│   └── [slug]/
│       └── page.tsx          # 플러그인 상세 + 오늘의 브리핑
└── mypage/
    └── page.tsx              # 기존 + "구독 관리" 탭 추가
```

### 3.2 플러그인 목록 페이지 (`/plugins`)

```
┌─────────────────────────────────────────────────────────┐
│  🍳 SideDish Plugins                                    │
│  매일 아침, 당신에게 필요한 정보를 요리해드려요         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │ ☀️ 오늘의   │  │ 📈 코인     │  │ 📰 IT뉴스   │     │
│  │ 날씨        │  │ 브리핑      │  │ 요약        │     │
│  │             │  │             │  │             │     │
│  │ 매일 7시    │  │ 매일 8시    │  │ 매일 9시    │     │
│  │             │  │             │  │             │     │
│  │ [구독하기]  │  │ [구독하기]  │  │ [구독중 ✓]  │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 3.3 플러그인 상세 페이지 (`/plugins/weather`)

```
┌─────────────────────────────────────────────────────────┐
│  ☀️ 오늘의 날씨                                         │
│  어제와 비교한 오늘의 날씨, AI가 분석해드려요           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  📍 도시 선택                                           │
│  ┌────────┐ ┌────────┐ ┌────────┐                      │
│  │ 서울 ✓ │ │ 부산   │ │ 대구   │  ...                 │
│  └────────┘ └────────┘ └────────┘                      │
│                                                         │
│  ⏰ 발송 시간: 매일 오전 7시                            │
│                                                         │
│  [구독하기]                                             │
│                                                         │
├─────────────────────────────────────────────────────────┤
│  📋 오늘의 브리핑 미리보기                              │
│                                                         │
│  어제보다 3도 떨어진 영하 1도로 시작하는 아침이에요.   │
│  체감온도는 영하 5도까지 내려가니 두꺼운 패딩은 필수!  │
│  ...                                                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 3.4 마이페이지 구독관리 탭

```
┌─────────────────────────────────────────────────────────┐
│  [내 메뉴] [찜한 메뉴] [받은 귓속말] [구독 관리]        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  구독 중인 플러그인 (2/5)                               │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ ☀️ 오늘의 날씨                                   │   │
│  │ 📍 서울 · ⏰ 매일 7시                            │   │
│  │                                    [설정] [해제] │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 📰 IT뉴스 요약                                   │   │
│  │ ⏰ 매일 9시                                      │   │
│  │                                    [설정] [해제] │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [+ 더 많은 플러그인 보기]                              │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 4. 날씨 플러그인 상세 설계

### 4.1 데이터 흐름

```
[Cron Job - 매일 6:50 KST]
        │
        ▼
[1. 날씨 API 호출]
   OpenWeatherMap API
   - 주요 도시별 현재 날씨
   - 어제 날씨 (히스토리 API 또는 캐시)
   - 오늘 예보
        │
        ▼
[2. 데이터 통합]
   {
     seoul: { yesterday: {...}, today: {...}, forecast: {...} },
     busan: { ... },
     ...
   }
        │
        ▼
[3. AI 분석 - 1회 호출]
   Gemini 2.5 Flash Lite
   - 모든 도시 데이터 입력
   - 도시별 종합 브리핑 생성
        │
        ▼
[4. 결과 캐시 저장]
   plugin_logs에 저장
   (같은 날 재요청 시 캐시 사용)
        │
        ▼
[5. 이메일 발송 - 7:00 KST]
   구독자별로 선택한 도시의 브리핑 발송
```

### 4.2 AI 프롬프트

```typescript
const WEATHER_PROMPT = `
당신은 SideDish 플랫폼의 날씨 브리핑 에디터입니다.

## 입력 데이터
다음은 주요 도시의 어제와 오늘 날씨 데이터입니다:
${JSON.stringify(weatherData, null, 2)}

## 출력 형식
각 도시별로 다음 JSON 형식으로 브리핑을 작성해주세요:

{
  "seoul": {
    "summary": "한 줄 요약 (이메일용, 50자 이내)",
    "briefing": "종합 브리핑 (마크다운, 200자 내외)"
  },
  "busan": { ... },
  ...
}

## 브리핑 작성 가이드
1. 어제 대비 오늘 기온 변화 언급
2. 옷차림 추천 (구체적으로)
3. 우산 필요 여부
4. 미세먼지/자외선 주의사항 (해당 시)
5. 마무리 한마디 (따뜻한 톤)

톤앤매너: 친근하고 따뜻하게, 해요체 사용
금지: 상투적 표현, 과도한 이모지
`;
```

### 4.3 이메일 템플릿

```html
<!-- 요약 모드 (기본) -->
<div style="font-family: sans-serif; max-width: 600px; margin: 0 auto;">
  <h1 style="color: #4F46E5;">📬 오늘의 SideDish 브리핑</h1>
  <p style="color: #64748B;">2024년 12월 19일 목요일</p>

  <div style="background: #F8FAFC; padding: 20px; border-radius: 12px; margin: 20px 0;">
    <h2>☀️ 서울 날씨</h2>
    <p style="font-size: 18px; color: #1E293B;">
      어제보다 3도 ↓, 두꺼운 패딩 필수
    </p>
  </div>

  <a href="https://sidedish.app/plugins/weather"
     style="display: inline-block; background: #4F46E5; color: white;
            padding: 12px 24px; border-radius: 8px; text-decoration: none;">
    👉 자세히 보기
  </a>

  <hr style="margin: 30px 0; border: none; border-top: 1px solid #E2E8F0;">

  <p style="color: #94A3B8; font-size: 12px;">
    이 메일은 SideDish 플러그인 구독으로 발송되었습니다.<br>
    <a href="https://sidedish.app/mypage">구독 관리</a>
  </p>
</div>
```

---

## 5. 이메일 발송 시스템

### 5.1 Resend 설정

```typescript
// src/lib/resend.ts
import { Resend } from 'resend'

export const resend = new Resend(process.env.RESEND_API_KEY)

export async function sendPluginEmail({
  to,
  subject,
  html,
}: {
  to: string
  subject: string
  html: string
}) {
  return resend.emails.send({
    from: 'SideDish <noreply@sidedish.app>',
    to,
    subject,
    html,
  })
}
```

### 5.2 환경 변수 추가

```bash
# .env.local
RESEND_API_KEY=re_xxxxx

# 날씨 API
OPENWEATHERMAP_API_KEY=xxxxx

# Cron 시크릿 (보안용)
CRON_SECRET=xxxxx
```

---

## 6. 스케줄링

### 6.1 Vercel Cron Jobs

```json
// vercel.json
{
  "crons": [
    {
      "path": "/api/cron/plugins/weather",
      "schedule": "50 21 * * *"  // UTC 21:50 = KST 06:50
    }
  ]
}
```

### 6.2 Cron 엔드포인트

```typescript
// src/app/api/cron/plugins/weather/route.ts

export async function GET(request: Request) {
  // 1. Cron 시크릿 검증
  const authHeader = request.headers.get('authorization')
  if (authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
    return new Response('Unauthorized', { status: 401 })
  }

  // 2. 날씨 데이터 수집
  const weatherData = await fetchWeatherData()

  // 3. AI 브리핑 생성
  const briefings = await generateWeatherBriefing(weatherData)

  // 4. 구독자에게 이메일 발송
  const subscribers = await getPluginSubscribers('weather')
  await sendBriefingEmails(subscribers, briefings)

  // 5. 로그 저장
  await savePluginLog('weather', { ... })

  return Response.json({ success: true })
}
```

---

## 7. 구현 순서 (MVP)

### Phase 1: 기반 구조 (Day 1)
- [ ] 타입 정의 (`src/lib/plugin-types.ts`)
- [ ] Firestore 컬렉션 상수 추가
- [ ] 플러그인 API 엔드포인트 (CRUD)
- [ ] 구독 API 엔드포인트

### Phase 2: UI 구현 (Day 2)
- [ ] 플러그인 목록 페이지 (`/plugins`)
- [ ] 플러그인 카드 컴포넌트
- [ ] 구독 버튼/모달
- [ ] 마이페이지 구독관리 탭

### Phase 3: 날씨 플러그인 (Day 3)
- [ ] OpenWeatherMap API 연동
- [ ] AI 브리핑 생성 서비스
- [ ] 브리핑 미리보기 API
- [ ] 플러그인 상세 페이지

### Phase 4: 이메일 발송 (Day 4)
- [ ] Resend 설정
- [ ] 이메일 템플릿 컴포넌트
- [ ] 발송 로직 구현
- [ ] Vercel Cron 설정

### Phase 5: 테스트 & 마무리 (Day 5)
- [ ] E2E 테스트 (구독 → 발송)
- [ ] 에러 핸들링 보강
- [ ] 로깅/모니터링
- [ ] 문서화

---

## 8. 파일 구조 (최종)

```
src/
├── app/
│   ├── plugins/
│   │   ├── page.tsx                    # 플러그인 목록
│   │   └── [slug]/
│   │       └── page.tsx                # 플러그인 상세
│   ├── api/
│   │   ├── plugins/
│   │   │   ├── route.ts                # GET (목록), POST (생성)
│   │   │   └── [id]/
│   │   │       ├── route.ts            # GET, PATCH, DELETE
│   │   │       └── preview/route.ts    # GET (미리보기)
│   │   ├── subscriptions/
│   │   │   ├── route.ts                # GET (내 구독), POST (구독)
│   │   │   └── [id]/route.ts           # PATCH, DELETE
│   │   └── cron/
│   │       └── plugins/
│   │           └── weather/route.ts    # Cron 실행
│   └── mypage/page.tsx                 # 구독관리 탭 추가
│
├── components/
│   ├── plugins/
│   │   ├── PluginCard.tsx              # 플러그인 카드
│   │   ├── PluginSubscribeModal.tsx    # 구독 설정 모달
│   │   ├── SubscriptionCard.tsx        # 구독 카드 (마이페이지용)
│   │   └── WeatherPreview.tsx          # 날씨 미리보기
│   └── emails/
│       └── PluginBriefingEmail.tsx     # 이메일 템플릿
│
├── lib/
│   ├── plugin-types.ts                 # 플러그인 타입 정의
│   ├── resend.ts                       # Resend 클라이언트
│   └── api-client.ts                   # 플러그인 API 함수 추가
│
└── services/
    ├── weatherService.ts               # OpenWeatherMap API
    └── pluginBriefingService.ts        # AI 브리핑 생성
```

---

## 9. 보안 고려사항

1. **Cron 엔드포인트**: `CRON_SECRET` 헤더 검증
2. **Admin 전용 API**: Firebase Custom Claims로 관리자 권한 체크
3. **구독 제한**: 사용자당 최대 5개
4. **Rate Limiting**: 구독/해제 API에 적용
5. **이메일 검증**: Firebase Auth 이메일 사용

---

## 10. 확장 계획

### 향후 추가 플러그인
- 📈 코인/주식 브리핑
- 📰 IT 뉴스 요약
- 💱 환율 알림
- 📅 일정 리마인더

### 확장 기능
- 사용자 플러그인 생성 (Phase 2)
- 플러그인 마켓플레이스
- 유료 플러그인 결제 연동
- 푸시 알림 옵션

---

이 계획으로 진행해도 될까요?
